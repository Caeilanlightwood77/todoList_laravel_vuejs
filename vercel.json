{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": { "distDir": "dist" }
      }
    ],
    "scripts": {
      "build": "composer install --no-dev --optimize-autoloader && npm run build"
    }
  }
  